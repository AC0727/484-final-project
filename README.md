# 484-final-project
CAM and Object Detection


## Set Up
This project runs using a `.venv`. 

To set up the environment, generate your virtual environment using `python3 -m venv .venv`. Then, activate it using `source .venv/bin/activate`.

To install the required libraries, run `pip install -r requirements.txt`